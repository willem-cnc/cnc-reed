; definition for locations for reed production

o<machine> sub

;
; definitions for tool length touch plate
;
   #<_x-touch>  =  85
   #<_y-touch>  =  145                        ; point on touch plate
   #<_z-touch>  = -67                         ; no tool injected 
;
; definitions for inside (gouging & modelling)
;
   #<_ix-origin>  =  36.000                  ; middle of reed carrier
   #<_iy-origin>  =  -3.500                  ; start of reed
   #<_iz-origin>  = -37.100                  ; top of reed carrier
   #<_iz-probe>   = -35.363                  ; probe touch value
;
; definitions for profiling
;
   #<_px-origin>  =  136.000                 ; middle of reed carrier
   #<_py-origin>  =   6.500                  ; start of reed
   #<_pz-origin>  = -31.800                  ; top of reed carrier
   #<_pz-probe>   = -35.363                  ; probe touch value
;
;common definitions
;
   #<_pipe-radius>   = 12.5                  ; outside radius of reed
   #<_b-radius>      = 13                    ; radius of blade roll
   #<_i-sink-z>      = .60                   ; reed sinks in carrier
   #<_i-delta-z>     = 0                     ; difference depth front back

o<machine> endsub

   M2    ; programm end
