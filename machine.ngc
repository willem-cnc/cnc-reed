; definition for locations for reed production

o<machine> sub

;
; base definitions for machine locations
;
; definitions for tool length touch plate
;
   #<_x-touch>    =  194.000
   #<_y-touch>    =  170.000                ; point on touch plate
   #<_z-touch>    =  -56.830                ; contact without tool
;
; definitions for inside (gouging & modelling)
;
   #<_ix-origin>  =   31.000                 ; middle of reed carrier
   #<_iy-origin>  =   29.900                 ; free from clamp
   #<_iz-origin>  =  -44.450                 ; top of reed carrier
;
; definitions for profiling
;
   #<_px-origin>  =  194.000                 ; middle of reed carrier
   #<_py-origin>  =   10.350                 ; start of reed
   #<_pz-origin>  =  -29.650                 ; top of reed carrier
; 
; probe-z during measurement
;
   #<_z-probe>    =  -31.734                 ; probe touch value
;
;common definitions
;
   #<_pipe-radius>   =  12.5                 ; outside radius of reed
   #<_b-radius>      =  12.5                 ; radius of blade roll
   #<_bed-length>    = 140                   ; length for gouging bed
   #<_scratch-size>  =   8                   ; space for gouging clips
   #<_i-sink-z>      =   1.00                ; reed sinks in carrier
;
; tool definitions (corresponding with linuxcnc tools)
;
   #<_ball-nose-0.5>  =   1
   #<_ball-nose-0.75> =   2
   #<_ball-nose-1>    =   3
   #<_ball-nose-1.25> =   4
   #<_ball-nose-1.5>  =   5
   #<_ball-nose-1.75> =   6
   #<_ball-nose-2>    =   7
   #<_finger-2.45>    =  11
   #<_probe>          = 100

o<machine> endsub

   M2    ; programm end
