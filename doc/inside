Inside has variables

gouge-margin      for testing normally 0
model-margin      room above reed carrier during modelling
_scratch-size     room for clamping the reed to the carrier

The programm does
- reads machine and reed parameters 
- setup x,y for inside carrier
   if gouging flag is set
      - select tool 3 for gouging
      - meassure tool offset
      - calls gouging
   if modelling flag is set
      - select tool 11 for modelling
      - meassure tool offset
      - multiple calls modelling 
